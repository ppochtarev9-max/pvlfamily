# 📓 Журнал Разработки (DevLog)

## Сессия: Экспорт данных и работа над ошибками компиляции

**Дата:** 2026-04-17
**Цель:** Реализовать выгрузку данных в Excel через интерфейс iOS приложения.

## Сессия: Экспорт данных и работа над ошибками компиляции

**Дата:** 2026-04-17
**Цель:** Реализовать выгрузку данных в Excel через интерфейс iOS приложения.

### Хронология действий:

1. **Бэкенд (Успех):**
  - Реализованы эндпоинты `/budget/export/excel` и `/tracker/export/excel`.
  - Добавлена фильтрация по датам, разделение категорий на колонки, стилизация таблиц.
  - Установлена зависимость `openpyxl==3.1.5`.
  - *Инцидент:* Первая версия сломала сборку из-за несовместимости аргументов `TableStyleInfo`. Исправлено откатом стиля.
2. **iOS Интеграция (Провал -> Успех):**
  - **Попытка 1:** Прямое добавление кнопок в `ProfileView.swift`.
    - *Ошибка:* Компилятор Swift выдал `Generic parameter 'Content' could not be inferred` и `unable to type-check`. Причина: перегрузка типа в сложном `body`.
  - **Решение:** Откат к рабочей версии. Вынос логики экспорта в отдельный экран `ExportDataView.swift`.
  - **Результат:** Создан отдельный файл с выбором типа данных и периода. Навигация через `NavigationDestination`.
  - *Мелкая правка:* Иконка `doc.badge.arrow.down` заменена на универсальную `square.and.arrow.down` (iOS 17+ проблема).
3. **Деплой на сервер (Инцидент):**
  - Сервис упал с 502 ошибкой после обновления `requirements.txt`.
  - *Причина:* Скрипт деплоя не обновил зависимости в venv автоматически.
  - *Решение:* Ручная установка пакетов на сервере.

### Итоги сессии:

- ✅ Экспорт Бюджета и Сна работает.
- ✅ Интерфейс вынесен в отдельный экран `ExportDataView`.
- ❌ Неудачный опыт с усложнением `ProfileView`.

### Уроки (Lessons Learned):

1. **Правило "Разделяй и властвуй":** Если компилятор тупит на типизации в большом `body` — немедленно выноси функциональность в отдельный View.
2. **Проверка иконок:** Использовать только базовые SF Symbols или проверять версию iOS.
3. **Деплой зависимостей:** Всегда проверять логи сервиса (`systemctl status`) после изменения `requirements.txt`. Автоматика может молча пропустить установку.
4. **Сложные алерты:** Избегать `.constant(...)` в сложных иерархиях, использовать явные `@State`.

---

### Хронология действий:

1. **Бэкенд (Успех):**
  - Реализованы эндпоинты `/budget/export/excel` и `/tracker/export/excel`.
  - Добавлена фильтрация по датам, разделение категорий на колонки, стилизация таблиц.
  - Установлена зависимость `openpyxl==3.1.5` для поддержки форматирования.
  - *Инцидент:* Первая версия сломала сборку из-за несовместимости аргументов `TableStyleInfo`. Исправлено откатом стиля до базового.
2. **iOS Интеграция (Провал -> Успех):**
  - **Попытка 1:** Прямое добавление кнопок и логики в `ProfileView.swift`.
    - *Ошибка:* Компилятор Swift выдал ошибку `Generic parameter 'Content' could not be inferred` и `The compiler is unable to type-check this expression`. Причина: перегрузка типа в сложном вложенном `body` с множеством `.alert` и `.sheet`.
    - *Попытка исправления:* Разбиение кода на части не помогло сразу, возник цикл ошибок.
  - **Решение:** Откат к рабочей версии `ProfileView.swift`. Вынос логики экспорта в отдельный экран `ExportDataView.swift`.
  - **Результат:** Создан отдельный файл `ExportDataView.swift` с выбором типа данных (Бюджет/Сон) и периода. Навигация реализована через `NavigationDestination`.
  - *Мелкая правка:* Иконка `doc.badge.arrow.down` не найдена в системе (требует iOS 17+). Заменена на универсальную `square.and.arrow.down`.
3. **Деплой на сервер (Инцидент):**
  - После обновления `requirements.txt` сервис на cloud.ru упал с ошибкой 502.
  - *Причина:* Скрипт деплоя не обновил зависимости в виртуальном окружении сервера автоматически.
  - *Решение:* Ручная установка пакетов через `pip install -r requirements.txt` внутри venv на сервере.

### Итоги сессии:

- ✅ Экспорт Бюджета работает (проверено).
- ✅ Экспорт Сна готов к тестированию.
- ✅ Интерфейс вынесен в отдельный экран, код стабилен.
- ❌ Неудачный опыт с усложнением `ProfileView` без выделения в отдельный View.

### Уроки (Lessons Learned):

1. **Правило "Разделяй и властвуй":** Если `View` становится сложным или компилятор начинает "тупить" на типизации — немедленно выноси новую функциональность в отдельный файл/экран. Не пытайся впихнуть всё в один `body`.
2. **Проверка иконок:** Всегда проверять доступность SF Symbols для целевой версии iOS. Если сомневаешься — используй классику (`square.and.arrow.down`).
3. **Деплой зависимостей:** Автоматические скрипты могут молча пропускать установку новых пакетов. После изменения `requirements.txt` всегда проверяй логи сервиса на сервере (`systemctl status`).
4. **Сложные алерты:** Избегать использования `.constant(...)` в `.alert` внутри сложных иерархий, если это вызывает ошибки вывода типов. Лучше использовать явные `@State` булевы флаги.

---

*Следующий шаг: Тестирование экспорта сна и переход к задачам безопасности (SEC-04).*

---

## Сессия: Актуализация процесса разработки и проектного контекста

**Дата:** 2026-04-23
**Запрос:** Зафиксировать строгие правила работы, обновить контекст/бэклог, добавить отдельный файл good/bad кейсов и отметить важность проверки импорт-скрипта исторических транзакций.

### Выполнено

1. **Контекст проекта (`PROJECT_CONTEXT.md`):**
  - Добавлен блок про исторические данные и скрипт `backend/import_history.py`.
  - Добавлены обязательные правила работы:
    - только через четкий план;
    - без регрессий;
    - не удалять без подтверждения;
    - не чистить БД без явного указания;
    - обновлять `DEV_LOG.md` после каждого запроса/фичи;
    - тесты для новых модулей/фич.
2. **Бэклог (`BACKLOG.md`):**
  - Добавлена задача `DATA-05` на проверку импорт-скрипта истории транзакций.
  - Добавлен раздел "Процесс и качество" (`PROC-01..PROC-04`) для дисциплины планирования, тестирования и фиксации решений.
3. **Файл кейсов (`GOOD_BAD_CASES.md`):**
  - Создан отдельный журнал good/bad решений.
  - Добавлена первая запись про восстановление контракта `groups/subcategories` в `main`.

### Риски/наблюдения

- Без регулярной проверки `import_history.py` можно получить скрытую несовместимость после изменений в моделях категорий.
- Жесткие процессные правила полезны, но требуют дисциплины в каждом запросе и commit-цикле.

---

## Сессия: Формализация чек-листа разработки

**Дата:** 2026-04-23
**Запрос:** Добавить отдельный файл с детальными правилами процесса разработки и чек-листами по этапам.

### Выполнено

1. Создан `DEVELOPMENT_RULES.md` с обязательными правилами и пошаговым процессом:
  - до начала задачи,
  - во время реализации,
  - перед коммитом,
  - перед пушем,
  - после пуша/релиза.
2. Добавлен единый `Definition of Done (DoD)` для завершения задач.
3. Обновлен `PROJECT_CONTEXT.md` ссылкой на `DEVELOPMENT_RULES.md`.

### Практический эффект

- Меньше импровизации в разработке.
- Проще контролировать регрессии и качество изменений.
- Единый понятный стандарт для всех следующих задач.

---

## Сессия: DATA-05 — проверка импорта истории транзакций

**Дата:** 2026-04-23
**Запрос:** Проверить импорт исторических транзакций из CSV рядом со скриптом и убедиться в совместимости с `groups/subcategories`.

### Выполнено

1. Полностью обновлен `backend/import_history.py` под текущую модель:
  - использование `CategoryGroup` + `Category(group_id=...)`,
  - отказ от legacy-полей `parent_id/type` у `Category`,
  - добавлен безопасный CLI:
    - `--dry-run`,
    - `--replace-user-transactions`,
    - `--user`,
    - `--csv`.
2. Убрана опасная логика массовой очистки категорий.
3. Выполнен dry-run на файле `backend/history.csv`:
  - командой `cd backend && ./venv/bin/python import_history.py --csv history.csv --dry-run`
  - результат: импортируемых транзакций `142`, пропущенных строк `5078`,
  - запись в БД не выполнялась.

### Наблюдения

- Большое число пропусков связано с форматом CSV (валидных строк с датой в формате `dd.mm.yyyy` немного).
- Для боевого импорта сначала лучше согласовать:
  - нужно ли удалять существующие транзакции пользователя (`--replace-user-transactions`),
  - достаточно ли текущих правил пропуска строк.

### Продолжение (подтвержденный боевой импорт)

- По подтверждению пользователя выполнен вариант `2`:
  - `cd backend && ./venv/bin/python import_history.py --csv history.csv --user "Паша" --replace-user-transactions`
- Результат выполнения:
  - успешно сохранено в БД,
  - импортировано `142` транзакции,
  - пропущено `5078` строк.
- Контрольная проверка БД:
  - у пользователя `Паша` (`user_id=1`) в `transactions` теперь `142` записи.

---

## Сессия: SEC-05 — старт аудита уязвимостей API

**Дата:** 2026-04-23
**Запрос:** После закрытия DATA-05 начать аудит безопасности API (доступы, утечки, валидация, rate limit).

### Статус

- `DATA-05` переведен в `✅ Готово` (разовая миграционная задача).
- `SEC-05` переведен в `🟢 В работе`.

### Предварительные findings (по коду)

1. **Критично:** в `budget.py` часть `transactions`-эндпоинтов не требует `get_current_user` и не фильтрует данные по владельцу (чтение/изменение/удаление).
2. **Критично:** в `auth.py` endpoints `GET /auth/users` и `DELETE /auth/users/{user_id}` доступны без авторизации.
3. **Важно:** в `stats.py` при `user_id=None/0` возвращаются агрегаты по всем пользователям, что может вести к нежелательной видимости данных.
4. **Важно:** в `calendar.py` `GET /events` открыт без авторизации, а `DELETE /events/{event_id}` разрешен любому авторизованному пользователю.

### Следующий шаг

- Согласовать и внести remediation-пакет (минимальные безопасные правки без ломки UX): закрыть неавторизованные endpoints и ограничить доступ к чужим данным.

---

## Сессия: Усиление аутентификации (закрытая регистрация)

**Дата:** 2026-04-23
**Запрос:** Убрать вход "по любому имени", добавить парольную аутентификацию, создание пользователей только через admin-механизм, forced password reset для `Паша` и поддержку этого сценария в iOS.

### Что реализовано

1. **Backend auth:**
  - `User` расширен полями: `password_hash`, `is_active`, `is_admin`, `must_reset_password`.
  - `/auth/login` теперь принимает `name + password`, не создает пользователя автоматически.
  - Единая ошибка при неверных данных: `"Неверные учетные данные"`.
  - Добавлены endpoints:
    - `POST /auth/admin/users` (создание пользователя только по `X-Admin-Token`),
    - `POST /auth/change-password` (сброс временного пароля после первого входа).
  - `/auth/users` и `/auth/users/{id}` закрыты авторизацией (delete — только для admin).
2. **Bootstrap admin через `.env`:**
  - Добавлены env-параметры `ADMIN_NAME`, `ADMIN_INITIAL_PASSWORD`, `ADMIN_TOKEN`.
  - На старте приложения выполняется bootstrap админа (`Паша`):
    - `is_admin=True`,
    - `must_reset_password=True`.
  - Добавлена легкая sqlite-миграция auth-колонок в `users` на старте.
3. **iOS:**
  - `LoginView` обновлен на форму `имя + пароль`.
  - Добавлен экранный сценарий forced reset пароля при первом входе.
  - `AuthManager` обновлен:
    - login с паролем,
    - состояние `requiresPasswordReset`,
    - метод `changePassword(newPassword:)`.
4. **Тесты:**
  - Обновлены `backend/tests/conftest.py` и `backend/tests/test_auth.py` под новую auth-модель.
  - Прогон: `18 passed` (`test_auth`, `test_budget`, `test_tracker`).

### Локальная операционная заметка

- На машине создан локальный `.env` (игнорируется git) с `SECRET_KEY`, `ADMIN_TOKEN`, `ADMIN_NAME=Паша`, временным `ADMIN_INITIAL_PASSWORD`.

### Уточнение по env (после обратной связи)

- Убран дублирующий корневой `.env`.
- Конфигурация аутентификации перенесена в единый `backend/.env`.
- В `auth.py` зафиксирована приоритетная загрузка `backend/.env` для предсказуемого поведения.

---

## Сессия: Сброс бюджетных данных на cloud и реимпорт истории

**Дата:** 2026-04-24

**Контекст:** после успешного логина и смены пароля на сервере — полная очистка таблиц бюджета (группы, подкатегории, транзакции) и повторный импорт из `history.csv` скриптом `backend/import_history.py`.

**Порядок операций (на сервере):** остановить `pvlfamily` → `DELETE` из `transactions`, `categories`, `category_groups` (в таком порядке) → положить CSV в `backend/` → `cd backend && ./venv/bin/python import_history.py --user "Паша" --csv history.csv` → `systemctl start pvlfamily`. Пользователи (`users`), календарь, трекер сна в этом сценарии **не** трогаем.

**Миграция `categories.group_id` (2026-04-24):** если на сервере осталась старая таблица `categories` без `group_id`, `import_history` падает с `no such column: categories.group_id`. В `main.py` добавлен `ensure_budget_schema()`: при старте API обнаруживает отсутствие `group_id`, дропает `transactions`, `categories`, `category_groups` и пересоздаёт их через `create_all`.
# Конфигурация для COMMANDS.md
# Этот файл НЕ должен попадать в репозиторий (добавлен в .gitignore)

# Локальная разработка
LOCAL_PROJECT_PATH=/Users/Pavel/PVLFamily
LOCAL_VENV_PATH=venv

# Удаленный сервер (Cloud.ru)
REMOTE_IP=213.171.28.80
REMOTE_USER=user1
REMOTE_SSH_KEY=~/.ssh/pvl_server_key
REMOTE_APP_DIR=/home/user1/pvl_app/backend
REMOTE_SERVICE_NAME=pvlfamily

# Пути на сервере
REMOTE_APP_ROOT=/home/user1/pvl_app

HEALTH_ENDPOINT=http://$REMOTE_IP:8000/health